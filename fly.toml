# fly.toml — Lyftr demo instance
# Set app name below, then: fly secrets set JWT_SECRET=<random-32-chars>
# First deploy: fly deploy
# See fly/SETUP.md for full instructions.

app = "lyftr-backend-alexis"
primary_region = "fra"

[build]
  dockerfile = "fly/Dockerfile"

[env]
  ENV        = "production"
  PORT       = "3000"
  DB_PATH    = "/app/data/lyftr.db"
  CORS_ORIGIN = "https://lyftr-rat.web.app"

# JWT_SECRET must be set as a secret:
#   fly secrets set JWT_SECRET=$(openssl rand -hex 32)

[http_service]
  internal_port       = 80
  force_https         = true
  auto_stop_machines  = "stop"
  auto_start_machines = true
  min_machines_running = 0

  [http_service.concurrency]
    type       = "connections"
    hard_limit = 100
    soft_limit = 80

[[mounts]]
  source      = "lyftr_data"
  destination = "/app/data"

[[vm]]
  memory   = "512mb"
  cpu_kind = "shared"
  cpus     = 1
